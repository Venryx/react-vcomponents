"use strict";
/*import React from "react";
import {BaseComponent, BaseProps, AddGlobalStyle, ApplyBasicStyles, E, ClassBasedStyles} from "react-vextensions";
import {Row} from "..";

var styles = {
    root: {
    },
};

export type ButtonBarProps = {enabled?: boolean} & React.HTMLAttributes<HTMLDivElement>;

@ApplyBasicStyles
export class ButtonBar extends BaseComponent<ButtonBarProps, {}> {
    static defaultProps = {enabled: true};
    
    render() {
        var {enabled, style, ...rest} = this.props;
    
        let finalStyle = E(
            styles.root,
            style,
        );

        return (
            <Row {...rest} style={finalStyle}>
            </Row>
        );
    }
}*/ 
